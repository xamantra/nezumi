import 'package:flutter/material.dart';

class AnimeItemGrid extends StatelessWidget {
  const AnimeItemGrid({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
